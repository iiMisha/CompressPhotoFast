# Android Optimization Analyzer - Инструкции для агентов

## Назначение

Этот документ предназначен для скилла `/agents-updater` и содержит инструкции для обновления AGENTS.md после использования скилла `android-optimization-analyzer`.

## Когда обновлять AGENTS.md

Обновить AGENTS.md после:

1. **Завершения анализа производительности** - добавлены новые метрики или SLOs
2. **Обнаружения критических проблем** - найдены memory leaks, OOM crashes, ANR
3. **Рекомендаций по рефакторингу** - предложены изменения архитектуры
4. **Изменений в background processing** - WorkManager, Services, JobScheduler
5. **Внедрения инструментов мониторинга** - LeakCanary, StrictMode, Profiler
6. **Оптимизаций работы с изображениями** - Bitmap, decode, compress, recycle

## Что обновлять в AGENTS.md

### Section: Current Focus

**Добавить найденные проблемы:**
```markdown
### Известные проблемы
- 🔴 **[Краткое название]** - [Описание]
  **Файлы:** [список файлов]
  **Метрики:** [если есть]
```

**Пример:**
```markdown
### Известные проблемы
- 🔴 **Memory leak в ImageCompressionWorker** - удерживает Bitmap после сжатия
  **Файлы:** ImageCompressionWorker.kt:145-152
  **Метрики:** LeakCanary deteced, +15MB heap size
```

### Section: Последние изменения

**Добавить выполненную работу:**
```markdown
- ✅ [Краткое описание]
```

**Примеры:**
```markdown
- ✅ Добавлен LeakCanary для детекция утечек памяти
- ✅ Включён StrictMode в debug builds
- ✅ Оптимизирован decode изображений (inSampleSize)
- ✅ Исправлен coroutine scope leak в BackgroundMonitoringService
```

### Section: Known Issues

**Если проблема уже известна - обновить статус:**
```markdown
### 🔴 [Название проблемы]

**Статус:** 🟡 В работе / ✅ Исправлено

**Обновление:** [Что сделано]
```

### Section: Tech Stack (если изменился)

**Добавить новые зависимости:**
```markdown
### Android
- ... (существующее)
- LeakCanary 2.12 (debugImplementation)
- androidx.benchmark:benchmark-junit4:1.2.0 (androidTestImplementation)
```

### Section: Architecture

**Добавить новые компоненты:**
```markdown
### Monitoring
- PerformanceMonitor.kt - отслеживание метрик сжатия
- MemoryLeakDetector.kt - автоматическая детекция утечек
```

### Section: Testing Coverage

**Добавить новые тесты:**
```markdown
### Команды тестирования
```bash
# Performance тесты
./gradlew :app:connectedCheck -Pandroid.testInstrumentationRunnerArguments.class=com.compressphotofast.benchmark.CompressionBenchmark
```
```

## Шаблон отчёта для AGENTS.md

После завершения анализа используй следующий шаблон для обновления AGENTS.md:

```markdown
## Android Optimization Analysis - [Дата]

### Найденные проблемы
- 🔴 **[Проблема 1]**
  **Location:** `path/to/file.kt:line`
  **Impact:** [влияние]
  **Solution:** [решение]

- 🟡 **[Проблема 2]**
  **Location:** `path/to/file.kt:line`
  **Impact:** [влияние]
  **Solution:** [решение]

### Рекомендации
1. [Приоритет 1]
2. [Приоритет 2]

### Следующие шаги
- [ ] [Действие 1]
- [ ] [Действие 2]

### Обновления зависимостей (если есть)
- [Новые библиотеки или инструменты]
```

## Интеграция с /agents-updater

При запуске `/agents-updater` скилл `android-optimization-analyzer` передаст этот файл для автоматического обновления AGENTS.md.

**Ключевые слова для поиска:**
- `performance`
- `memory`
- `optimization`
- `leak`
- `OOM`
- `ANR`
- `Bitmap`
- `coroutine`
- `Worker`
- `background`

## Пример полного обновления

**Было:**
```markdown
## Current Focus (Февраль 2026)

### Известные проблемы
- 🔴 **Дубликаты при массовой обработке (50+ файлов)** - проблема с URI
```

**Стало:**
```markdown
## Current Focus (Февраль 2026)

### Известные проблемы
- 🔴 **Дубликаты при массовой обработке (50+ файлов)** - проблема с URI
- 🔴 **Memory leak в ImageCompressionWorker** - удерживает Bitmap после сжатия
  **Файлы:** ImageCompressionWorker.kt:145-152
  **Метрики:** LeakCanary deteced, +15MB heap size

### Последние изменения
- ✅ Цветовые коды в конфигурациях агентов
- ✅ Включена сборка покрытия тестов в debug сборке
- ✅ Добавлен MCP сервер для context7
- ✅ Миграция на Unified AI Configuration (.agents/rules/)
- ✅ Симлинки для всех AI-IDE (.claude/.cursor/.gemini/.kilocode/.opencode/)
- ✅ Добавлен LeakCanary для детекция утечек памяти
- ✅ Включён StrictMode в debug builds
- ✅ Оптимизирован decode изображений (inSampleSize)
- ✅ Исправлен coroutine scope leak в BackgroundMonitoringService
```
