---
name: android-test-suite
description: Runs all Android tests (unit + instrumentation), monitors logcat via adb, analyzes test failures, and fixes code errors. Use when user asks to run tests, fix failing tests, or debug Android application issues.
metadata:
  version: "1.0"
  author: compressphotofast
compatibility: Requires Android SDK, adb, Gradle, and Android emulator or connected device
---

# Android Test Suite Runner

Этот скилл предназначен для запуска всех тестов Android приложения, мониторинга logcat через adb и исправления найденных ошибок.

## Когда использовать этот скилл

Используйте этот скилл, когда:
- Пользователь просит запустить все Android тесты
- Необходимо проверить работу приложения после изменений
- Тесты падают и нужно исправить ошибки
- Требуется проанализировать logcat для диагностики проблем
- Нужно проверить coverage кода

## Что делает этот скилл

1. **Проверка и подготовка окружения**
   - Проверка наличия подключенного устройства/эмулятора
   - Автоматический запуск эмулятора Small_Phone при необходимости
   - Проверка готовности устройства (Android 10+)

2. **Запуск тестов**
   - Unit тесты (`testDebugUnitTest`) - быстрые тесты без Android framework
   - Instrumentation тесты (`connectedDebugAndroidTest`) - тесты на устройстве
   - Генерация coverage отчета (JaCoCo)

3. **Мониторинг logcat**
   - Сбор логов приложения во время выполнения тестов
   - Фильтрация ошибок, предупреждений и крашей
   - Анализ stack trace для диагностики проблем

4. **Исправление ошибок**
   - Анализ упавших тестов и их причин
   - Исправление кода для прохождения тестов
   - Повторный запуск тестов для проверки исправлений

## Начало работы

### Шаг 1: Подготовка окружения

Проверьте наличие подключенного устройства:

```bash
adb devices
```

Если устройство не найдено, запустите эмулятор:

```bash
./scripts/start_emulator.sh Small_Phone
```

Или используйте автоматический запуск:

```bash
./scripts/check_device.sh --start-emulator
```

### Шаг 2: Запуск тестов с мониторингом logcat

Сначала запустите сбор logcat в фоновом режиме для отслеживания ошибок в реальном времени:

```bash
# Очистка logcat перед запуском тестов
adb logcat -c

# Запуск logcat в фоновом режиме с сохранением в файл
adb logcat -v time > test_run_logcat.log 2>&1 &
LOGCAT_PID=$!

# Запуск всех тестов
./scripts/run_all_tests.sh --start-emulator

# Остановка logcat после завершения тестов
kill $LOGCAT_PID
```

### Шаг 3: Анализ результатов

**Успешное выполнение:**
- Все тесты прошли без ошибок
- Coverage отчет сгенерирован в `app/build/reports/jacoco/jacocoCombinedTestReport/html/index.html`

**Ошибки в тестах:**

1. Проверьте вывод Gradle для поиска stack trace
2. Проанализируйте logcat для runtime ошибок:
   ```bash
   # Фильтрация ошибок из logcat
   grep -E "(FATAL|AndroidRuntime|System.err)" test_run_logcat.log
   ```
3. Найдите упавшие тесты в HTML отчетах:
   - Unit: `app/build/reports/tests/testDebugUnitTest/index.html`
   - Instrumentation: `app/build/reports/androidTests/connected/index.html`

### Шаг 4: Исправление ошибок

Для каждого упавшего теста:

1. **Прочитайте тестовый файл** - используйте Read tool
2. **Проанализируйте ошибку**:
   - В сообщении об ошибке будет указано, что именно пошло не так
   - Stack trace покажет место в коде, где произошла ошибка
3. **Найдите тестируемый код** - используйте Grep/Glob для поиска файлов
4. **Исправьте код** - используйте Edit tool
5. **Соберите проект** - используйте Bash tool:
   ```bash
   ./gradlew assembleDebug
   ```
6. **Перезапустите тесты** - используйте TodoWrite для отслеживания прогресса

### Шаг 5: Повторная проверка

После исправления всех ошибок:

```bash
# Запуск всех тестов для проверки
./scripts/run_all_tests.sh
```

## Частые сценарии исправления

### Сценарий 1: AssertionError в unit тестах

**Проблема:** Тест ожидает определенное значение, но получает другое

**Решение:**
1. Прочитайте тестовый файл и найдите assertion
2. Проверьте тестируемый код на логические ошибки
3. Исправьте код или обновите тест (если тест неверен)

**Пример:**
```kotlin
// Тест падает с: expected:<true> but was:<false>
@Test
fun testCompressionEnabled() {
    assertThat(settingsManager.isCompressionEnabled()).isTrue()
}

// Проверьте код SettingsManager.isCompressionEnabled()
// Исправьте логику или обновите тест если ожидание неверное
```

### Сценарий 2: NoAliveProcess в instrumentation тестах

**Проблема:** Эмулятор или приложение крашится

**Решение:**
1. Проверьте logcat для stack trace краша
2. Ищите FATAL EXCEPTION или AndroidRuntime ошибки
3. Исправьте проблему в коде (например, NPE, неправильные приведения типов)

**Пример:**
```bash
# В logcat найдете:
FATAL EXCEPTION: main
java.lang.NullPointerException: Attempt to invoke virtual method
  on a null object reference
  at com.compressphotofast.util.ImageCompressionUtil.compress()

# Исправьте NPE в ImageCompressionUtil.kt
```

### Сценарий 3: Timeout в тестах

**Проблема:** Тест выполняется слишком долго

**Решение:**
1. Проверьте, нет ли бесконечных циклов
2. Увеличьте timeout в тесте (если тест корректный)
3. Оптимизируйте код для быстродействия

### Сценарий 4: Permission denial

**Проблема:** Нет необходимых разрешений

**Решение:**
1. Проверьте `AndroidManifest.xml` - добавьте разрешения
2. Для instrumentation тестов используйте `@GrantPermissionRule`
3. Для unit тестов используйте mock объектов

**Пример:**
```kotlin
@get:Rule
val grantPermissionRule = GrantPermissionRule.grant(
    Manifest.permission.READ_EXTERNAL_STORAGE,
    Manifest.permission.WRITE_EXTERNAL_STORAGE
)
```

## Полезные команды

### Запуск тестов

```bash
# Все тесты (unit + instrumentation) с авто-запуском эмулятора
./scripts/run_all_tests.sh --start-emulator

# Только unit тесты (быстро)
./gradlew testDebugUnitTest

# Только instrumentation тесты
./gradlew connectedDebugAndroidTest

# Запуск конкретного теста
./gradlew testDebugUnitTest --tests "com.compressphotofast.util.SettingsManagerTest"

# Запуск конкретного instrumentation теста
./gradlew connectedDebugAndroidTest --tests "com.compressphotofast.e2e.SettingsE2ETest"
```

### Coverage и отчеты

```bash
# Проверка покрытия кода
./gradlew jacocoTestReport

# Открытие coverage отчета
xdg-open app/build/reports/jacoco/jacocoTestReport/html/index.html
```

### Сборка

```bash
# Очистка проекта
./gradlew clean

# Полная пересборка
./gradlew clean assembleDebug

# Сборка в eco режиме (меньше нагрузка на CPU)
GRADLE_MODE=eco ./gradlew assembleDebug
```

### Logcat

```bash
# Мониторинг logcat в реальном времени
adb logcat | grep -E "(FATAL| compressphotofast)"

# Просмотр последних ошибок из logcat
adb logcat -d | grep -A 20 "FATAL"

# Очистка logcat
adb logcat -c

# Сохранение logcat в файл
adb logcat -v time > logcat_output.log
```

### Поиск в коде

```bash
# Поиск всех тестов для класса
grep -r "SettingsManager" app/src/test

# Поиск конкретного теста
grep -r "testAutoCompression" app/src/androidTest
```

## Структура тестовых файлов

```
app/src/
├── test/                                  # Unit тесты (без Android framework)
│   └── java/com/compressphotofast/
│       ├── BaseUnitTest.kt                # Базовый класс для unit тестов
│       ├── util/                          # Тесты утилит
│       │   ├── SettingsManagerTest.kt
│       │   ├── FileOperationsUtilTest.kt
│       │   └── ...
│       └── performance/                   # Performance тесты
│           └── CompressionLoadTest.kt
│
└── androidTest/                           # Instrumentation тесты (с Android framework)
    └── java/com/compressphotofast/
        ├── BaseInstrumentedTest.kt        # Базовый класс для instrumentation тестов
        ├── BaseE2ETest.kt                 # Базовый класс для E2E тестов
        ├── e2e/                           # Сквозные тесты
        │   ├── SettingsE2ETest.kt
        │   ├── BatchCompressionE2ETest.kt
        │   └── AutoCompressionE2ETest.kt
        ├── util/                          # Тесты утилит на устройстве
        │   ├── HeicFormatTest.kt
        │   └── ...
        └── heic/                          # Тесты HEIC обработки
            └── HeicInstrumentationTest.kt
```

## Базовые классы для тестов

### BaseUnitTest

Базовый класс для unit тестов с CoroutinesTestRule:

```kotlin
abstract class BaseUnitTest {
    @get:Rule
    val coroutinesTestRule = CoroutinesTestRule()

    // Helper методы для создания mock объектов
    // Общая настройка для всех unit тестов
}
```

### BaseInstrumentedTest

Базовый класс для instrumentation тестов:

```kotlin
@HiltAndroidTest
abstract class BaseInstrumentedTest {
    @get:Rule
    val hiltRule = HiltAndroidRule(this)

    @get:Rule
    val activityRule = ActivityScenarioRule(MainActivity::class.java)

    // Общая настройка для всех instrumentation тестов
}
```

### BaseE2ETest

Базовый класс для E2E тестов:

```kotlin
@HiltAndroidTest
abstract class BaseE2ETest : BaseInstrumentedTest() {
    // Специфичная настройка для E2E тестов
    // Permissions, UI тесты, etc.
}
```

## Советы по исправлению

1. **Используйте TodoWrite** для отслеживания прогресса исправления тестов
2. **Используйте Read tool** перед редактированием файлов - никогда не редактируйте файл, который не прочитали
3. **Делайте по одной fix за шаг** для упрощения отладки
4. **Запускайте сборку после каждого изменения** кода (согласно правилам проекта)
5. **Используйте grep** для поиска связанных тестов:
   ```bash
   grep -r "ClassName" app/src/test
   grep -r "testMethodName" app/src/test
   ```

## Процесс работы скилла

Когда этот скилл активирован, выполните следующие шаги:

1. **Создайте todo list** для отслеживания прогресса
2. **Проверьте устройство** - запустите check_device.sh
3. **Запустите logcat** в фоновом режиме
4. **Выполните тесты** - запустите run_all_tests.sh
5. **Остановите logcat** после завершения тестов
6. **Проанализируйте результаты**:
   - Если тесты прошли → сообщите об успехе
   - Если есть падения → перейдите к шагу 7
7. **Для каждого упавшего теста**:
   - Прочитайте тестовый файл
   - Проанализируйте ошибку и logcat
   - Найдите проблемный код
   - Исправьте код
   - Соберите проект
   - Отметьте todo как completed
8. **Перезапустите тесты** для проверки всех исправлений
9. **Сообщите итоговый результат**

## Пример работы скилла

```
🎯 Запуск Android Test Suite...

1. ✅ Проверка устройства...
   Устройство: emulator-5554 (Android 13, API 33)

2. 📊 Запуск unit тестов...
   ✅ 42 теста пройдено за 45 секунд

3. 📱 Запуск instrumentation тестов...
   ❌ 3 теста упали:
      - SettingsE2ETest.testAutoCompression
      - BatchCompressionE2ETest.testBatchCompression
      - HeicInstrumentationTest.testHeicFileProcessing

4. 🔍 Анализ logcat...
   Найдена ошибка: NullPointerException в ImageCompressionUtil.compress():145

5. 🔧 Исправление ошибок...
   ✅ ImageCompressionUtil.kt:145 - добавлена проверка null
   ✅ BatchCompressionWorker.kt:78 - исправлена обработка результатов
   ✅ HeicFileProcessor.kt:23 - добавлена валидация формата

6. 🔄 Повторный запуск тестов...
   ✅ Все 48 тестов пройдены!

7. 📈 Coverage: 12.4% (+4.2%)

✨ Все тесты успешно прошли!
```

## Дополнительная документация

- **Правила разработки:** `.kilocode/rules/rules.md`
- **Контекст проекта:** `.kilocode/rules/memory-bank/context.md`
- **Инструкция по тестированию:** `.kilocode/rules/rules.md` (раздел Тестирование)
- **Gradle Eco Mode:** `docs/GRADLE_ECO_MODE.md`
- **Скрипты запуска тестов:** `scripts/run_all_tests.sh`
- **Скрипты проверки устройства:** `scripts/check_device.sh`
- **Скрипты запуска эмулятора:** `scripts/start_emulator.sh`

## Требования окружения

- Android SDK с настроенным ANDROID_HOME
- ADB (Android Debug Bridge)
- Gradle (через Gradle wrapper)
- Эмулятор Android Small_Phone или физическое устройство
- Python 3.10+ (для некоторых скриптов)

## Известные проблемы

1. **Медленный запуск эмулятора** - может занимать 1-2 минуты
2. **Timeout на медленных машинах** - используйте eco режим: `GRADLE_MODE=eco`
3. **Несколько устройств** - при подключении нескольких устройств выберите нужное через `-s` флаг
