# Универсальная схема организации AI-агентов

## 📁 Структура

```
project-root/
├── AGENTS.md                    # Контекст проекта
├── .agents/                     # Source of Truth
│   ├── rules/rules.md           # Правила для AI
│   ├── agents/*.md              # Агенты
│   └── skills/*/SKILL.md        # Скиллы
└── .{ide}/                      # Симлинки → .agents/
```

---

## 📄 AGENTS.md (корень проекта)

Контекст проекта, читается в начале каждой задачи.

**Секции:** Project Overview, Tech Stack, Architecture, Key Features, Code Style, Current Focus, Known Issues, Testing Coverage, Version Update.

---

## 📄 .agents/rules/rules.md

Правила работы AI-агентов.

**Обязательные правила:**
1. Читать AGENTS.md в начале каждой задачи
2. Использовать агентов/скиллы для изменений кода
3. Выполнять сборку после изменений
4. Запускать тесты через Task tool

---

## 🤖 Агент (.agents/agents/*.md)

```markdown
---
name: agent-name
description: [Когда использовать]
tools: Read, Write, Edit, Bash, Glob, Grep
---
[Системный промпт]
```

---

## 🎯 Скилл (.agents/skills/*/SKILL.md)

```markdown
---
name: skill-name
description: [Описание]
user-invocable: true
arguments:
  - name: arg
    description: [Описание]
    required: true/false
---
[Инструкции и ограничения]
```

---

## 🔗 Симлинки

Для каждой IDE (claude, cursor, kilocode, gemini, opencode) создать:
- `.{ide}/rules/` → симлинк на `.agents/rules/`
- `.{ide}/agents/` → симлинки на файлы из `.agents/agents/`
- `.{ide}/skills/` → симлинки на директории из `.agents/skills/`

---

## 💡 Принципы

- **Единый источник:** всё в `.agents/`
- **Симлинки:** IDE ссылаются на `.agents/`
- **AGENTS.md:** до 200 строк, актуальность
- **Агенты:** чёткое описание, инструменты
- **Скиллы:** явные ограничения
