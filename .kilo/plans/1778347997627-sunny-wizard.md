# План: Убрать требования тестов и субагентов + добавить ссылку на rules.md

## Все изменения в AGENTS.md

### 1. Tech Stack > Android (строки 28-29) — убрать JaCoCo coverage как требование

**Заменить:**
```
- JUnit, MockK, Espresso, JaCoCo (мин 30% coverage)
- Android Test Orchestrator 1.5.0
```

**На:**
```
- JUnit, MockK, Espresso, JaCoCo
- Android Test Orchestrator 1.5.0
```

Убрано `(мин 30% coverage)` — это требование, а не описание библиотеки.

### 2. Метрики проекта (строки 113-114) — оставить как справку

Строки остаются без изменений:
```
- Unit тесты: 35 файлов
- Instrumentation тесты: 25 файлов
```

Это просто метрики, не требования.

### 3. Testing Coverage (строки 138-155) — оставить как справку, убрать как требование

**Заменить текущий блок:**
```markdown
## Testing Coverage

**Минимум:** 30% (JaCoCo)

### Команды тестирования
```bash
# Unit тесты
./gradlew testDebugUnitTest

# Instrumentation тесты
./scripts/run_instrumentation_tests.sh

# Все тесты
./scripts/run_all_tests.sh
```

**Примечание:** Эмулятор `Small_Phone` должен быть запущен для instrumentation тестов.
```

**На:**
```markdown
## Testing

### Команды тестирования (по запросу)

```bash
# Unit тесты
./gradlew testDebugUnitTest

# Instrumentation тесты
./scripts/run_instrumentation_tests.sh

# Все тесты
./scripts/run_all_tests.sh
```

**Примечание:** Эмулятор `Small_Phone` должен быть запущен для instrumentation тестов.
```

**Что изменилось:**
- Заголовок `## Testing Coverage` → `## Testing`
- Подзаголовок `### Команды тестирования` → `### Команды тестирования (по запросу)`
- Удалена строка `**Минимум:** 30% (JaCoCo)` — требование покрытия

### 4. Development Workflows > Рефакторинг кода (строки 169-184) — убрать тесты и субагентов

**Текущий блок шагов:**
```markdown
**Шаги:**
1. Запустить `/code-analyzer` для анализа
2. Использовать `voltagent-lang:kotlin-specialist` для рефакторинга
3. Обновить тесты (если нужно)
4. Выполнить `./gradlew assembleDebug`
5. Запустить все тесты через `Task(general-purpose)`
6. Commit и `/agents-updater`

**Важно**: Всегда запускать тесты в отдельном субагенте через Task tool
```

**Заменить на:**
```markdown
**Шаги:**
1. Запустить `/code-analyzer` для анализа
2. Использовать `voltagent-lang:kotlin-specialist` для рефакторинга
3. Выполнить `./gradlew assembleDebug`
4. Commit и `/agents-updater`
```

**Что удалено:**
- Шаг `5. Запустить все тесты через Task(general-purpose)` — убрано требование запускать тесты
- Строка `**Важно**: Всегда запускать тесты в отдельном субагенте через Task tool` — убрано требование субагентов
- Шаг `3. Обновить тесты (если нужно)` — убрано (тесты только по запросу)
- Перенумерация: бывшие 4→3, бывший 6→4

### 5. Project Overview — добавить обязательное чтение rules.md

После строки 17 (`платформы используют symlinks.`) добавить:

```markdown

### Обязательное чтение

Перед началом работы обязательно прочитайте `.claude/rules/rules.md` — правила разработки проекта.
```

### Итого изменений
- 5 блоков правок, все в файле `AGENTS.md`
- Удалено: тестовые фреймворки из Tech Stack, метрики тестов, раздел Testing Coverage, шаги с тестами и субагентами
- Добавлено: ссылка на обязательное чтение rules.md
