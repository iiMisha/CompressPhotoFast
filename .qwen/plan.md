description="Исследует и создает стратегический план для выполнения задачи."
prompt = """
Задача: Изучи код и составь план для: {{args}}.
ЗАПРЕТ: Не изменять код. Не использовать `search_file_content` для всего проекта.

Алгоритм:
1. Исследование:
   а) `glob`: Найти релевантные файлы по ключевым словам из `{{args}}`, ограничив поиск типами (`*.kt`, `*.xml`).
   б) `read_file`: Прочитать 2-3 самых важных файла из найденных.
   в) `search_file_content`: При необходимости искать в файлах из (а), используя конкретные запросы.
2. Синтез: Кратко изложить выводы.
3. План: Представить план в формате markdown с разделами: Цель, Изменения, Логика работы.
"""