<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Основная тема приложения (светлая) -->
    <style name="Theme.CompressPhotoFast" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Цвета -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="colorSurface">@color/surface</item>
        
        <!-- Статус бар -->
        <item name="android:statusBarColor">@color/primary_dark</item>
        <item name="android:windowLightStatusBar">false</item>
        
        <!-- Фон -->
        <item name="android:windowBackground">@color/background</item>
        
        <!-- Типографика -->
        <item name="android:textColorPrimary">@color/primary_text</item>
        <item name="android:textColorSecondary">@color/secondary_text</item>
        
        <!-- Стиль ActionBar -->
        <item name="actionBarStyle">@style/Widget.CompressPhotoFast.ActionBar</item>
    </style>
    
    <!-- Стиль для ActionBar -->
    <style name="Widget.CompressPhotoFast.ActionBar" parent="Widget.MaterialComponents.ActionBar.Primary">
        <item name="titleTextStyle">@style/TextAppearance.CompressPhotoFast.ActionBar.Title</item>
    </style>
    
    <!-- Стиль для заголовка в ActionBar -->
    <style name="TextAppearance.CompressPhotoFast.ActionBar.Title" parent="TextAppearance.MaterialComponents.Headline6">
        <item name="android:textSize">16sp</item>
    </style>
    
    <!-- Стиль для кнопок -->
    <style name="Widget.CompressPhotoFast.Button" parent="Widget.MaterialComponents.Button">
        <item name="android:textColor">@color/white</item>
        <item name="android:padding">12dp</item>
        <item name="android:textSize">16sp</item>
    </style>
    
    <!-- Стиль для переключателей -->
    <style name="Widget.CompressPhotoFast.Switch" parent="Widget.MaterialComponents.CompoundButton.Switch">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/primary_text</item>
    </style>
</resources> 