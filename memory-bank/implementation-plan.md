# План реализации исправления логики замены файлов для Intent

## Анализ проблемы

### Выявленные проблемы

1. **Ограниченные права доступа к внешним URI**
   - Файлы, полученные через Intent, имеют ограниченные права доступа
   - Приложение может только читать эти файлы, но не может их модифицировать или удалять
   - `FileOperationsUtil.renameOriginalFileIfNeeded()` пропускает обработку для MediaDocuments URI

2. **Логическая несогласованность**
   - `MediaStoreUtil.saveCompressedImageFromStream()` корректно определяет директорию на основе `isSaveModeReplace`
   - Но переименование/удаление оригинала не происходит из-за ограничений доступа
   - Результат: файл сохраняется в правильной директории, но оригинал остается нетронутым

3. **Отсутствие альтернативной стратегии**
   - Нет механизма для обработки случаев, когда прямая замена невозможна
   - Нет уведомления пользователя о том, что замена не удалась

### Корневая причина

В методе `FileOperationsUtil.renameOriginalFileIfNeeded()` есть проверка:

```kotlin
if (uri.authority == "com.android.providers.media.documents") {
    LogUtil.processDebug("Пропускаем переименование для MediaDocuments URI: $uri")
    return
}
```

Эта проверка блокирует переименование для всех URI из галереи, включая те, что приходят через Intent.

## Стратегия решения

### Подход 1: Улучшенная обработка MediaDocuments URI

1. **Преобразование URI**
   - Использовать `UriUtil.convertMediaDocumentsUri()` для преобразования в content:// URI
   - Попытаться выполнить операции с преобразованным URI

2. **Проверка возможности модификации**
   - Добавить метод для проверки, можно ли модифицировать файл по URI
   - Учесть различия между локальными файлами и файлами из Intent

3. **Альтернативная стратегия**
   - Если прямая замена невозможна, сохранить в директории приложения
   - Добавить метаданные о том, что файл является заменой оригинала

### Подход 2: Контекстно-зависимая обработка

1. **Флаг источника**
   - Передавать информацию о том, откуда пришел файл (Intent vs локальная обработка)
   - Применять разные стратегии в зависимости от источника

2. **Уведомления пользователя**
   - Информировать пользователя, когда замена невозможна
   - Предложить альтернативные действия

## Детальный план реализации

### Фаза 1: Создание тестового приложения

**Статус:** ✅ Завершено
- [x] Создано тестовое приложение TestIntentSender
- [x] Реализована отправка одного и нескольких изображений
- [x] Настроена конфигурация проекта

### Фаза 2: Тестирование текущего поведения

**Цель:** Подтвердить проблему и понять текущее поведение

**Шаги:**
1. Собрать и установить тестовое приложение
2. Протестировать отправку изображений с включенной настройкой замены
3. Протестировать отправку изображений с выключенной настройкой замены
4. Документировать наблюдаемое поведение

**Ожидаемые результаты:**
- С включенной заменой: файлы сохраняются в Pictures/, но оригиналы не удаляются
- С выключенной заменой: файлы сохраняются в Pictures/CompressPhotoFast/

### Фаза 3: Реализация исправлений

#### 3.1 Улучшение FileOperationsUtil.renameOriginalFileIfNeeded()

**Изменения:**
```kotlin
suspend fun renameOriginalFileIfNeeded(context: Context, uri: Uri, isFromIntent: Boolean = false): Boolean {
    if (!isSaveModeReplace()) {
        return false
    }
    
    // Преобразуем MediaDocuments URI в content URI
    val effectiveUri = UriUtil.convertMediaDocumentsUri(uri) ?: uri
    
    // Для файлов из Intent проверяем возможность модификации
    if (isFromIntent && !canModifyFile(context, effectiveUri)) {
        LogUtil.processWarning("Невозможно заменить файл из Intent: $uri")
        return false
    }
    
    // Остальная логика...
}
```

#### 3.2 Добавление метода проверки возможности модификации

**Новый метод:**
```kotlin
private fun canModifyFile(context: Context, uri: Uri): Boolean {
    return try {
        // Попытка получить права на запись
        context.contentResolver.takePersistableUriPermission(
            uri, 
            Intent.FLAG_GRANT_WRITE_URI_PERMISSION
        )
        true
    } catch (e: Exception) {
        false
    }
}
```

#### 3.3 Обновление цепочки вызовов

**Изменения в ImageCompressionWorker:**
```kotlin
// Передаем информацию о том, что файл пришел из Intent
val renamed = FileOperationsUtil.renameOriginalFileIfNeeded(
    context, 
    originalUri, 
    isFromIntent = inputData.getBoolean("isFromIntent", false)
)
```

**Изменения в MainActivity.handleIntent():**
```kotlin
// Добавляем флаг при запуске обработки
val inputData = Data.Builder()
    .putString("imageUri", uri.toString())
    .putBoolean("forceProcess", true)
    .putBoolean("isFromIntent", true) // Новый флаг
    .build()
```

#### 3.4 Улучшение пользовательского опыта

**Добавление уведомлений:**
```kotlin
if (isFromIntent && !renamed && isSaveModeReplace()) {
    // Показать уведомление о том, что замена невозможна
    NotificationUtil.showReplacementNotPossible(context, originalFileName)
}
```

### Фаза 4: Тестирование исправлений

**Тестовые сценарии:**

1. **Intent с одним изображением (замена включена)**
   - Ожидаемый результат: Попытка замены, уведомление если невозможно

2. **Intent с одним изображением (замена выключена)**
   - Ожидаемый результат: Сохранение в директории приложения

3. **Intent с несколькими изображениями (замена включена)**
   - Ожидаемый результат: Попытка замены для каждого, сводное уведомление

4. **Intent с несколькими изображениями (замена выключена)**
   - Ожидаемый результат: Сохранение всех в директории приложения

5. **Локальная обработка (для регрессионного тестирования)**
   - Ожидаемый результат: Работа как раньше

### Фаза 5: Документирование и финализация

**Задачи:**
1. Обновить документацию по обработке Intent
2. Добавить комментарии к измененному коду
3. Обновить tasks.md с результатами
4. Создать заметки для будущих улучшений

## Технические детали

### Ключевые файлы для изменения

1. **FileOperationsUtil.kt**
   - Метод `renameOriginalFileIfNeeded()`
   - Новый метод `canModifyFile()`

2. **ImageCompressionWorker.kt**
   - Передача флага `isFromIntent`
   - Обработка результата переименования

3. **MainActivity.kt**
   - Добавление флага при создании задачи
   - Возможно, обработка уведомлений

4. **NotificationUtil.kt** (если нужно создать)
   - Уведомления о невозможности замены

### Риски и ограничения

1. **Ограничения Android**
   - Невозможно модифицировать файлы других приложений без специальных разрешений
   - Scoped Storage ограничивает доступ к файлам

2. **Пользовательский опыт**
   - Пользователи могут не понимать, почему замена не работает
   - Нужны четкие объяснения в уведомлениях

3. **Совместимость**
   - Изменения не должны нарушать существующую функциональность
   - Нужно тестирование на разных версиях Android

## Критерии успеха

1. **Функциональность**
   - Настройка замены корректно применяется для Intent
   - Пользователь получает понятную обратную связь
   - Существующая функциональность не нарушена

2. **Пользовательский опыт**
   - Понятные уведомления о результатах операций
   - Предсказуемое поведение в разных сценариях

3. **Техническое качество**
   - Код хорошо документирован
   - Обработка ошибок улучшена
   - Логирование помогает в отладке

## Следующие шаги

1. ✅ Создать тестовое приложение
2. 🔄 Собрать и протестировать текущее поведение
3. ⏳ Реализовать исправления по фазам
4. ⏳ Провести комплексное тестирование
5. ⏳ Документировать результаты